# Architecture Diagram

Add the exported diagram image as docs/architecture.png to match the repository structure.

Suggested flow:
IR/Ultrasonic Sensors -> Raspberry Pi (Edge Processing) -> Adaptive Timing Algorithm -> GPIO Control Logic -> Traffic Signal LEDs
